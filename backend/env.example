# ===========================================
# KOUNDOUL BACKEND - VARIABLES D'ENVIRONNEMENT
# ===========================================

# 🗄️ BASE DE DONNÉES
# Remplacez par votre URL Supabase
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# 🔐 AUTHENTIFICATION JWT
JWT_SECRET="koundoul-super-secret-jwt-key-2024-change-in-production"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# 🌐 SERVEUR
PORT=5000
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000"

# 🤖 GOOGLE AI (GEMINI)
GOOGLE_AI_API_KEY="your-google-ai-api-key-here"
GOOGLE_AI_MODEL="gemini-pro"  # Options: gemini-pro, gemini-1.5-pro (pas gemini-1.5-flash qui n'existe plus)
# Modèle pour parsing enrichi (Optionnel - utilise GOOGLE_AI_MODEL par défaut)
# Recommandé: gemini-1.5-pro pour meilleur raisonnement sur parsing
GOOGLE_AI_PARSER_MODEL="gemini-pro"  # Optionnel, fallback sur GOOGLE_AI_MODEL si non défini

# 💳 STRIPE PAYMENTS
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key_here"
STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"

# 📧 EMAIL (Optionnel - pour les notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# 🔒 SÉCURITÉ
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 📊 LOGGING
LOG_LEVEL="info"
LOG_FILE="logs/app.log"

# 🎯 GAMIFICATION
DEFAULT_XP_PROBLEM=10
DEFAULT_XP_QUIZ=20
DEFAULT_XP_BADGE=50
LEVEL_UP_THRESHOLD=100

# 💰 PAIEMENTS
CURRENCY="eur"
MIN_PAYMENT_AMOUNT=100
MAX_PAYMENT_AMOUNT=100000


