# Configuration Render pour Koundoul
# NOTE: Le backend n'est pas encore dans le repo Git
# Pour activer le backend:
# 1. Ajouter le dossier backend/ au repo Git
# 2. S'assurer que backend/package.json existe
# 3. Décommenter le service ci-dessous

# services:
#   - type: web
#     name: koundoul-backend
#     env: node
#     rootDir: backend
#     buildCommand: npm install && npx prisma generate
#     startCommand: npm start
#     envVars:
#       - key: NODE_ENV
#         value: production
#       - key: DATABASE_URL
#         sync: false
#       - key: JWT_SECRET
#         sync: false
#       - key: PORT
#         value: 5000

