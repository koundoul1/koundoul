# KOUNDOUL - VARIABLES D'ENVIRONNEMENT

# --- FRONTEND (Vite) ---
# URL de l'API backend (dev: http://localhost:5000, prod: https://xxx.onrender.com)
VITE_API_URL=http://localhost:5000

# --- BACKEND (Node) ---
# Voir backend/README.md pour DATABASE_URL, JWT_SECRET, FRONTEND_URL, etc.

# Supabase (micro-leçons) - optionnel, fallback statique si absent
# Récupérer l'URL et la clé anon dans Supabase → Project Settings → API
SUPABASE_URL=https://wnbkplyerizogmufatxb.supabase.co
SUPABASE_ANON_KEY=
